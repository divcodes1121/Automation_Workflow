"""Core building blocks shared across feature modules.

Reserved for cross-cutting concerns (base classes, shared protocols, pipeline
primitives) that more than one module in :mod:`backend.modules` depends on.
Populated in later phases.
"""
