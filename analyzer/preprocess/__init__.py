"""Preprocessing utilities for the Gameplay Analyzer.

Currently holds :mod:`analyzer.preprocess.build_templates`, which turns the
bundled card art into a cached template library. Future preprocessing (frame
extraction, ROI cropping) will live alongside it.
"""
